[pytest]
testpaths = tests
pythonpath = .
# Starlette's TestClient warns about httpx vs httpx2; test-client only, not app code.
filterwarnings =
    ignore:Using .httpx. with .starlette.testclient. is deprecated
