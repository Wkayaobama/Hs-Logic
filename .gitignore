# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
*.tsbuildinfo

# Secrets / local env
.env

# Editor telemetry
.specstory/
.cursorindexingignore

# OS
.DS_Store
Thumbs.db

# Built SPA copied next to the backend for local single-container checks; the image builds it itself
backend/static/
# Ansible deploy manifest (per-machine state)
deploy/ansible/last_deploy.json
